--- bash-completion-2.1/bash_completion.omv~	2014-05-05 14:06:57.266331344 +0200
+++ bash-completion-2.1/bash_completion	2014-05-05 14:08:00.373542395 +0200
@@ -717,7 +717,7 @@ _init_completion()
         fi
     done
 
-    [[ $cword -eq 0 ]] && return 1
+    [[ $cword -le 0 ]] && return 1
     prev=${words[cword-1]}
 
     [[ ${split-} ]] && _split_longopt && split=true
