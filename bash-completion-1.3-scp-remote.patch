diff -Naur -x '*~' -x '*.orig' -x '*.rej' bash-completion-1.3/completions/ssh bash-completion-1.3-scp-remote//completions/ssh
--- bash-completion-1.3/completions/ssh	2011-01-21 10:36:11.000000000 +0100
+++ bash-completion-1.3-scp-remote//completions/ssh	2011-02-07 15:39:09.000000000 +0100
@@ -253,6 +253,9 @@
 # only.  Returns paths escaped with three backslashes.
 _scp_remote_files()
 {
+    # return if COMP_SCP_REMOTE is not set
+    [ -n "{$COMP_SCP_REMOTE:-}" ] || return 0
+
     local IFS=$'\n'
 
     # remove backslash escape from the first colon
